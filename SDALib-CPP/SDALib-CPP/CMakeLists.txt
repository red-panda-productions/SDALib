
ADD_LIBRARY (SDALib STATIC "SDADriver.hpp" "SDAAction.hpp" "SDAData.hpp" "SDADriver.cpp")

include(GenerateExportHeader)
GENERATE_EXPORT_HEADER(SDALib)

TARGET_LINK_LIBRARIES(SDALib ${IPCLIB_LIBRARY})

TARGET_LINK_LIBRARIES(SDALib msgpackc-cxx)

TARGET_INCLUDE_DIRECTORIES(SDALib PUBLIC ${IPCLIB_INCLUDE_DIR} ${Boost_INCLUDE_DIR} ${CMAKE_BINARY_DIR}/SDALib-CPP) 

set_target_properties(SDALib PROPERTIES LINKER_LANGUAGE CXX)

INSTALL(TARGETS SDALib)
INSTALL(FILES ${SDALIB_HEADERS} ${CMAKE_BINARY_DIR}/SDALib-CPP/sdalib_export.h DESTINATION include)